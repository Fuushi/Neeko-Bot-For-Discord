print("Audio manager imported")

#make each game instance an object

#implement phone home on error

#platform independent file system will be required

#start with creating game object

class syncrnousCalls():
    def get_game_info(): #returns critical game info
        return()

class auGameInstance:
    print('update class call to start a thread with the audio support script')
    def __init__(self):
        #constructor class
        oTime = 0# time.time()
        guildID = ''
        channelID = '' #might be able to use only channel ID for DM
        ans = '' #pick dynamically
        hostUser = '' #
        gameID = ''

    def setParameters(guildIDimport, channelIDimport, hostUserimport):
        import time
        oTime = time.time()
        guildID = guildIDimport
        channelID = channelIDimport
        gameID = str(random.randint(1000,999999))
        hostUser = hostUserimport
        ans = 'call get ans' #call function for getting list of valid ans ret(list)

    def peekParameters():
        print('------------')
        print('peeking instance')
        print("GAM:    ", gameID)
        print("OT:     ", oTime)
        print("GID:    ", guildID)
        print("CID:    ", channelID)
        print("USR:    ", hostUser)





        #might use a separate function call to set parameters

        #call a check function on related messages

        #i check if i can define an asycnrynous function in a class
            #i will probly need asyncio for the first call side, find a way to pass completion status from second call side to first
